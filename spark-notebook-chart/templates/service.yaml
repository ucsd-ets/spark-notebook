apiVersion: v1
kind: Service
metadata:
  name: {{ include "spark-notebook-chart.fullname" . }}
  labels:
    {{- include "spark-notebook-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spark-notebook-chart.selectorLabels" . | nindent 4 }}

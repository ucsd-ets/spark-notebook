apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spark-notebook-chart.fullname" . }}-test-connection"
  labels:
    {{- include "spark-notebook-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spark-notebook-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
